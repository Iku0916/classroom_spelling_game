<% if flash.any? %>
  <div class="flash-message-container">
    <% flash.each do |type, message| %>
      <div class="flash <%= type == 'alert' ? 'error' : type %>">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>